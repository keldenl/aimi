---
interface Props {
  title: string;
  items: string[];
  icon: (_props: { size?: number; class?: string }) => unknown;
}

const { title, items, icon: IconComponent } = Astro.props;
---

<section class="rounded-[16px] border border-white/14 bg-white/[0.055] p-4 shadow-[inset_0_1px_0_rgba(255,255,255,0.05)] sm:p-5">
  <h2 class="mb-4 flex items-center gap-2 text-base font-black">
    <IconComponent size={20} class="text-ink" />
    {title}
  </h2>
  <div class="flex flex-wrap gap-2">
    {items.map((item) => (
      <span class="rounded-full bg-white/13 px-3 py-1.5 text-xs font-bold text-ink/90">{item}</span>
    ))}
  </div>
</section>
